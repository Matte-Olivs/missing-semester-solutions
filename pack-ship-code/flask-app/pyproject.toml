[project]
name = "string-saver"
version = "0.1.0"
description = "A simple string saver"
dependencies = ["flask>=3.0.2", "redis"]


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"